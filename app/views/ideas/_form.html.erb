<%= simple_form_for @idea, remote: true do |f| %>
  <%= f.input  :title,   as: :text             %>
  <%= f.input  :body,    as: :text             %>
  <%= f.input  :quality, as: :text, placeholder: "swill" %>
  <%= f.button :submit, value: "Save" %>
<% end %>
